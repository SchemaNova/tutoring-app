services:
  mysqldb:
    image: mysql:latest
    container_name: mysql-db
    restart: unless-stopped
    environment:
      DB_PASS: ${DB_PASS}
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
