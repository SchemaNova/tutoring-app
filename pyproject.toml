[project]
name = "db-final-prac"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cryptography>=46.0.2",
    "mysql-connector-python>=9.4.0",
]

[dependency-groups]
dev = [
    "ruff>=0.13.3",
]
